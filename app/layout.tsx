export const metadata = { title: 'Quiz IA Builder PRO', description: 'Monte seu quiz com IA — visual dinâmico por nicho.' };
import './globals.css';
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="pt-BR"><body>{children}</body></html>);
}
