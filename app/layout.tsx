export const metadata = { title: 'Quiz IA Builder', description: 'Monte seu quiz com IA — 100% seu.' };
import './globals.css';
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="pt-BR"><body>{children}</body></html>);
}
